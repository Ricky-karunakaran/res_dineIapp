<?xml version="1.0" encoding="UTF-8"?>
<project name="FoodVerse Restaurant" default="default" basedir="." xmlns:fx="javafx:com.sun.javafx.tools.ant">
    <description>Builds, tests, and runs the project Admin.</description>
    <import file="nbproject/build-impl.xml"/>
    
      <!-- <target name="-post-jfx-deploy">
       <fx:deploy width="${javafx.run.width}" height="${javafx.run.height}" 
                  nativeBundles="all"
                  outdir="${basedir}/${dist.dir}" outfile="${application.title}">
          <fx:application name="FoodVerse Alpha" 
                          mainClass="${javafx.main.class}">
          
          </fx:application>
          <fx:resources>
              <fx:fileset dir="${basedir}/${dist.dir}"
                          includes="*.jar"/>
              <fx:fileset dir="lib" includes="*.jar"/> 
                <fx:fileset dir="lib" includes="**/*.jar"/> 
          </fx:resources>
          <fx:info title="${application.title}" 
                   vendor="${application.vendor}">
          <fx:icon href="${basedir}/asset/FoodVerse.ico"/>
          </fx:info>
        </fx:deploy>          
     </target>   -->
    
   
</project>

